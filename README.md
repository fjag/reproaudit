# project2

A general-purpose Python project.

## Setup

```bash
# Install dependencies (using uv)
uv sync

# Run
uv run python -m project2.main

# Test
uv run pytest

# Lint / format
uv run ruff check .
uv run ruff format .
```
